import { makeStyles } from "@mui/styles";
const styles = makeStyles(() => ({
  paperStyle: {
    padding: 20,
    height: "40vh",
    width: 280,
    margin: "150px auto",
  },
  avatarStyle: {
    bgcolor: "black",
  },
}));

export default styles;
